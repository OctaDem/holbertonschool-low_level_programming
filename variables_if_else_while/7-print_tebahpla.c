#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char zwx = 'z';

	while (zwx >= 'a')

{

	putchar(zwx);

	zwx++;
}

	putchar('\n');

	return (0);
}
